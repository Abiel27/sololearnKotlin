//Fill in the blanks to output
//"Pass" if the grade variable is above 75, and "Fail", if not.
//(
fun main(args: Array<String>) {
    var grade = readLine()!!.toInt()
    if (grade > 75) {
        println("Pass")
    } else {
        println("Fail")


    }
}