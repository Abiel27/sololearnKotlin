//Arguments provide input to our functions.
//
//        For example, let's say we want to pass a name
//        as input to our welcome() function, so that the output included the provided name:

  fun welcome(name: String) {
          println("Hello, " + name)
          }
          fun main(args: Array<String>) {
              welcome("James")
              welcome("Amy")
          }
