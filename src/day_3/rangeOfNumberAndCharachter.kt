

//Kotlin allows you to easily create ranges of values using the following simple syntax:
//2..5 creates a range of numbers from 2 to 5.
fun main(args: Array<String>) {
    for (i in 2..5) {
        println(i)
    }
    for (x in 'a'..'e') {
        println(x)
    }
}
